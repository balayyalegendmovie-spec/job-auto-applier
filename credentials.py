# credentials.py
def get_credentials(portal):
    # Use input() for manual login. No password storing.
    print(f"Please log in to {portal} manually when prompted in the browser window.")
    return None  # Not needed for manual browser login

# For future: you can use getpass for CLI input, not stored in code.
